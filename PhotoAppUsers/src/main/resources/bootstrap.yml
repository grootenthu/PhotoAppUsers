spring:
  cloud:
    config:
      uri: http://localhost:8012
      name: users-ws
encrypt:
  key-store:
    location: file://${user.home}/keys/api/apiEncryptionKey.jks
    password: 1q2w3e4r
    alias: apiEncryptionKey