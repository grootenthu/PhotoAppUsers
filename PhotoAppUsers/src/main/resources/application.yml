eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
server:
  port: ${PORT:0}
spring:
  application:
    name: users-ws
  devtools:
    remote:
      restart:
        enabled: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    username: admin
    password: Admin@89
    url: jdbc:mysql://localhost:3306/photo_app?serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  security:
    user:
      name: admin
      password: admin
gateway:
  ip: 10.0.2.15
token:
  secret: lfjo3o4jojfojoj3ojojdfojksopkpppp
  expiration: 8650400403
login:
  url:
    path: /users/login
management:
  endpoints:
    web:
      exposure:
        include:
        - beans
        - httptrace
        - health
        - info
        - routes
    